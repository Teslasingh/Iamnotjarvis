# Install once (no venv): python3 -m pip install --user -r requirements.txt
fastapi
uvicorn
websockets
httpx
python-dotenv
python-multipart
croniter
# Merged from chat/: image/PDF analysis and SerpAPI web search (optional fallbacks).
PyMuPDF
google-search-results
openai
