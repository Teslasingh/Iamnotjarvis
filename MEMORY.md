# Agent memory

Durable learnings and environment notes.

## Learnings

- (2026-07-25) (2026-07-25) In this repository layout, persistent runtime state and conversation files live under `friday\\.friday\\` inside the repo root, not at the repository root itself. Use `get_system_info.friday_dir` before inspecting memory/state files to avoid invalid-path assumptions.
## Environment

